node_modules/
dist/
.DS_Store
*.local

# Original, unprocessed photos. Run `npm run photos:optimise` to generate
# the web-ready files in src/photos/.
photos-raw/

# Generated at build time by scripts/photo-dimensions.mjs.
src/photos/source-map.json

# Web-optimised copies of src/photos/, generated at build time by
# scripts/optimise-gallery-photos.mjs (including its own dimensions.json).
src/photos-optimised/
